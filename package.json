{
  "name": "sample-list-order",
  "type": "module",
  "devDependencies": {
    "sass": "^1.54.8",
    "typescript": "^4.8.2"
  },
  "scripts": {
    "watch": "npm run watch:sass & npm run watch:ts",
    "watch:sass": "sass src/scss/:dist/css/ --no-source-map --watch",
    "watch:ts": "tsc -watch"
  }
}
